import React,{useEffect,useState}from'react'
import {Search,SlidersHorizontal} from'lucide-react'
import Navbar from'../components/Navbar';import InternshipCard from'../components/InternshipCard';import api from'../services/api'
export default function Internships(){const[jobs,setJobs]=useState([]),[q,setQ]=useState(''),[source,setSource]=useState('all');const load=()=>api.get('/internships',{params:{q,limit:100}}).then(r=>setJobs(r.data.data)).catch(()=>{});useEffect(load,[q]);const list=source==='all'?jobs:jobs.filter(x=>(x.source||'').toLowerCase()===source);return <><Navbar/><main className="mx-auto max-w-7xl px-5 py-10"><div className="rounded-3xl bg-gradient-to-r from-indigo-50 to-white p-8"><p className="text-sm font-black tracking-widest text-brand">EXPLORE</p><h1 className="mt-3 text-4xl font-black">Find your next internship.</h1><p className="mt-3 text-slate-500">Search across InternSphere and permitted external sources.</p><div className="mt-7 flex max-w-3xl items-center gap-3 rounded-2xl bg-white p-3 shadow-sm ring-1 ring-slate-200"><Search className="text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} className="w-full outline-none" placeholder="Role, skill or company"/></div></div><div className="mt-8 flex flex-wrap items-center gap-3"><SlidersHorizontal size={18}/>{['all','aicte','internsphere'].map(s=><button key={s} onClick={()=>setSource(s)} className={`rounded-full px-4 py-2 text-sm font-bold ${source===s?'bg-brand text-white':'bg-slate-100'}`}>{s==='all'?'All':s.toUpperCase()}</button>)}</div><div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{list.map(j=><InternshipCard key={j._id} job={j}/>)}</div>{!list.length&&<div className="mt-8 rounded-3xl border bg-white p-12 text-center text-slate-500">No internships match your search.</div>}</main></>}
